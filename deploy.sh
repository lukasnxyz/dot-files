#!/bin/sh

# echo instructions on what to do next
# echo after each step what's begin done

# (check if current dir is dot-files) git clone this repo: https://github.com/lukasnxyz/dot-files

# cp -r config/nvim ~/.config/
# cp -r config/alacritty/ ~/.config/
# cp -r config/tmux ~/.config/
# cp -r config/zsh ~/.config/
# cp -r .zshenv ~/




# check if Ubuntu (apt) or Arch (pacman)
# sudo apt install -y neovim alacritty tmux zsh
# account for what's installed via snap and via apt/pacman

# change user shell to zsh






#
# Configs
# - nvim, alacritty, tmux, zsh
#
# Packages (based on system)
# - neovim, alacritty, tmux, zsh
